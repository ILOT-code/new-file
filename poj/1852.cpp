#include<cstring>
using namespace std;
const int maxn=1000000+5;
int main(){
    int case,len,num,a[maxn];
    scanf("%d",&case);
    while(case){
        scanf("%d%d",&len,&num);
        for(int i=0;i<num;++i) scanf("%d",a+i);
        f

    }
}